server {
    listen {{ ports.http }} ;
    listen [::]:{{ ports.http }} ;
    server_name {{ serv_subdom }}.{{ domain }} ;

    location / {
        limit_req zone=custom_limit burst=4 nodelay;
        limit_req_status 429;
        proxy_pass          http://127.0.0.1:{{ serv_port }};
    }
}
